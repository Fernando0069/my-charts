INFO: El chart "CyberChef" despliega la aplicación "CyberChef" dedicada a la trasnformación de información (a base64, epoch, etc.).
INFO: Despliega los siguiente objetos:
        - Imagestream.
        - Secret (generic webhook).
        - Secret (github webhook).
        - BuildConfig.
        - Deployment.
        - HorizontalPodAutoscaler.
        - PodDisruptionBudget.
        - Service.
        - Route.