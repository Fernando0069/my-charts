INFO: El chart "CyberChef" despliega una aplicación para la trasnformación de información (a base64, epoch, etc.).
INFO: Despliegalos siguiente objetos:
        - Imagestream.
        - Secret (generic webhook).
        - Secret (github webhook).
        - BuildConfig.
        - Deployment.
        - HorizontalPodAutoscaler.
        - PodDisruptionBudget.
        - Service.
        - Route.